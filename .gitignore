# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor
.vscode/
.idea/
*.swp
*.swo

# Build artifacts (kept in docs/ for GitHub Pages, but exclude local temp)
*.tmp

# Local-only agenda encryption tool and any plaintext agenda source.
# Only docs/agenda/agenda.enc (ciphertext) should ever be committed.
encrypt_agenda.py
research_agenda.html
research_agenda.csv

# Local-only loan-planner encryption tool + plaintext planner source + seeder.
# Only docs/planner/planner.enc (ciphertext) should ever be committed.
encrypt_planner.py
seed_planner.js
loan_planner_seeded.html

planner_apps_script.gs
